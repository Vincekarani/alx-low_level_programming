#include "lists.h"

/**
 * print_listint - prints all the elements of a listint_t list.
 * @h: linked list for type listint_t to be printef
 *
 * Return: the number of nodes
 */
size_t print_listint(const listint_t *h)
while
{
	size_t number = 0
		while (h)
		{
			printf("%dn", h->n)
				number++;
			return (number);
		}
}
